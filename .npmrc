# Fail fast on an unsupported local Node instead of only warning (EBADENGINE).
# The real transitive floor is Node >=22.19.0 (@earendil-works/pi-ai and
# pi-agent-core engines.node). See .nvmrc (canonical Node 24) and issue #42.
engine-strict=true
